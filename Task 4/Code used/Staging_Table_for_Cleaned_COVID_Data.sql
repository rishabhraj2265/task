CREATE TABLE stg_covid_worldwide (
    serial_number INTEGER,
    country TEXT,
    total_cases BIGINT,
    total_deaths BIGINT,
    total_recovered BIGINT,
    active_cases BIGINT,
    total_tests BIGINT,
    population BIGINT
);
